# （旧版本号：本地 admin 工具忽略规则已移除——新的 admin 是网页编辑器，
#  必须发布到 server。如果 admin_en.html 没建过就不存在，被列也无害。）

# 临时脚本
_*.py
_*.txt
_*.html
_*.css

# 源 Word 文档（网站只用 PDF 版）
*.docx

# 系统/IDE
~$*
Thumbs.db
.DS_Store
.idea/
.vscode/
*.swp

# 后端服务
server/node_modules/
server/data/
server/package-lock.json

# 部署密钥/.env
.env
.env.*

# 上传的成员照片（admin 页生成的，不进 git，避免 git pull 把 server 改的覆盖）
assets/img/team/
